{{!< default}}
{{#if data.ministries}}
	{{#each data.ministries}}
		{{#if ministryPage}}
			<h2>{{title}}</h2>
			<p>{{{time}}}</p>
			<p>
				{{#if description}}
					{{{description}}}
				{{/if}}
			</p>
			{{#if @last}} {{else}} <hr /> {{/if}}
		{{/if}}
	{{/each}}
{{/if}}